[package]
name = "nomi-coding"
description = "Coding harness: completion policy, prompts, progress/verify gates, todo continuation, compact prefs (Vetta-inspired completion + lean tools policy)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
