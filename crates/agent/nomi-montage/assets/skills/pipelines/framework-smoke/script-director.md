# Stage Director: script (framework-smoke)

Write a minimal valid `script` with at least one scene and non-empty
narration. Literary quality is irrelevant; schema validity and a usable
downstream handle are the only success criteria.
