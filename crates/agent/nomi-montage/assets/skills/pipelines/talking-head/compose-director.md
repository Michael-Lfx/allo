# Stage Director: compose (talking-head)

Finalize the approved talking-head clip into `renders/` via
`video_compose` / `video_stitch` as needed (even a single-clip project
still gets a canonical render_report under renders/).

Confirm the finalized file matches what was approved at
`talking_head_render`. Do not silently re-generate a different take at
compose time — regeneration belongs in the render stage with human
visibility.
