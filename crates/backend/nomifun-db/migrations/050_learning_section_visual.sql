-- 节级事实源补全：visual 声明落库（ADR-0002 追加决策的延续）。
--
-- 此前 visual（大纲声明的本节讲解主体可视化：公式/函数图/示意图/流程图/
-- 图表/表格/无）只活在生成期草稿——质检门按它做承诺兑现检查后即丢弃。
-- 落库后它是单节重写的承诺事实源：重写某一节时按原声明质检兑现，而不是
-- 凭空重猜。'' 为历史行/旧路径的「未声明」，质检按保守口径（要求任意可
-- 视化块）处理。
ALTER TABLE learning_lesson_sections ADD COLUMN visual TEXT NOT NULL DEFAULT '';
