# Stage Director: brief (framework-smoke)

Restate the project prompt into a schema-valid `brief`. Do not expand,
embellish, or invent research. `topic` and `key_message` must be
non-empty and clearly derived from the prompt.
