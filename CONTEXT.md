# Flowy 学习模块

本地学习引擎：课程图谱 + 课时分节内容 + 题目级 FSRS 复习调度。

术语体系继承自同作者的 learnhub-plugin（大愿景一致，本项目当前聚焦学习模块），两边词汇保持同源同步；差异处以本文件为准。

## Language

**Course（课程）**:
一个待学的完整学科，拥有自己的结构、题库与调度状态。
_Avoid_: 学科、专题

**Lesson（课时）**:
一次独立的学习行为单元，即一节完整的课。非原子知识点——除非该概念自身极其复杂。容量标准与 learnhub 一致：绝大多数课时应低于 30 分钟；只有不可拆分且非常复杂的课时才允许超过 30 分钟，但绝对不超过 60 分钟。（learnhub 中称 Node，两者同义；本项目沿用 Lesson 因代码已落地。）
_Avoid_: 知识点、课程

**Module（章）**:
课时的分组层级。learnhub 无此层，本项目保留 Course → Module → Lesson 三级。
_Avoid_: 节（易与 Section 混淆）、单元

**Section（节段）**:
课时内部的一个内容区块，由大纲阶段规划、逐个生成与质检；一个课时由若干节段组成。节段不可再分层。其数量、长度与类型配比应随课时复杂度弹性伸缩，而非固定。
_Avoid_: 小节、章节、模块、区块（易与 Block 混淆）

**Rewrite（单节重写）**:
只重新生成一个 Section 的正文的操作：其他节段与题目保持不动，该节按其大纲声明（含 visual 承诺）重新通过质检后原地替换。与整课重生成分属不同粒度——整课重生成替换全部节段与题库，Rewrite 只触碰一节。
_Avoid_: 重新生成（未指明粒度时）、修复

**Visual Promise（可视化承诺）**:
一个 Section 在大纲阶段声明的讲解主体可视化形态（公式/函数图/示意图/流程图/图表/表格/无），正文质检按声明兑现检查。几乎每节都有；「无」是留给确实无法可视化的纯推理/论述内容的合法声明，不是回避画图的出口。
_Avoid_: 图表承诺、可视化需求、配图

**Resume（续跑）**:
生成中断（失败/超时/重启放弃会话）后重试时，定位仍存活的草稿并从中断处继续，而非从零重建。已写好的节段与已建的图单元保留，新的模型会话经注入的进度记录恢复「做到哪了」的认知。
_Avoid_: 重试（未指明是否续跑）、断点恢复

**Complexity Tier（复杂度档位）**:
课时按难度、认知层级（bloom）与前置规模折叠出的内容规模级别，共三档（低/中/高）。内容生成据此推导节段数区间、篇幅预算与每节段题量。
_Avoid_: 档位（单独使用）、难度档、篇幅档

**Question（题目）**:
题库中的单题，自带一张 FSRS 调度卡，作答对错直接推进该卡。固定从属于一个课时，不跨课时复用。
_Avoid_: 试题、考题、练习

**Review Queue（复习队列）**:
到期题卡构成的跨课程扁平队列：全部启用课程中 due 不晚于今天的题，按预测遗忘风险分档升序（档内先易后难）逐张出示；不含从未调度过的新题。到期即可推进；当日尚未推进过的卡允许提前练一次（课程复习会话出示未到期卡）；当日已推进过的卡在重新到期前，重复作答（过期界面、并发提交）只记作答流水，不推进、不落日志。
_Avoid_: 复习列表、任务包、到期清单

**Forgot（忘记）**:
学习者申报「想不起来」：不作答直接翻面看答案，调度与统计均按答错记。受主动回忆门控——卡面展示一段时间后才允许申报，防止以申报代替回忆。
_Avoid_: 不会、放弃、跳过

**Review Log（复习日志）**:
调度卡每次真实推进的追加式记录：评分（1–4）、评分来源（自动判定/学习者自评/合成初始化）、复习间隔与推进前的记忆状态快照。它是「这张卡何时被推进过」的唯一事实源，记忆统计与参数优化都从它取数；作答流水不落在其中。
_Avoid_: 复习记录、作答记录（那是流水）、打卡

**Review Day（学习日）**:
调度与日志共用的「一天」边界：本地 02:00 翻日，00:00–01:59 记入前一个学习日。复习队列的「今天」、日志的学习日与打卡日是同一个日。
_Avoid_: 自然日、UTC 日

**Synthetic Push（合成推进）**:
完成课时等事件对尚未建卡的题目做的调度初始化：写卡并落一条合成日志，但不是真实作答——不构成推进，记忆统计与参数训练一律排除。
_Avoid_: 初始复习、预推、种卡

**True Retention（真实保留率）**:
到期复习中实际答对的比例：答错与忘记记失败，答对（含自评通过）记成功；每卡每个学习日只取第一次推进，合成推进排除。只读的调度诚实度度量，用于对照 FSRS 的自预测；不是 Mastery，不进门禁、不进任何账本。
_Avoid_: 正确率（那是作答口径）、保留率（单独使用易与 FSRS 预测混淆）

**Mastery（掌握度）**:
对课时当前掌握状态的连续度量（0–1），由其题目作答数据派生，无自评。目标是忠实表达学习者对该课时的实际掌握程度。
_Avoid_: 熟练度、分数、进度

**Stage（课时状态）**:
课时的离散流程状态（未开始/进行中/已完成/已跳过等）：记录学习者走到哪一步。与 mastery 各自独立演化。
_Avoid_: 进度、阶段

**Pre（前置）**:
课时之间的时序依赖边：学本课时之前应先掌握的课时。
_Avoid_: 依赖、前置条件

**Missing（缺失）**:
预期学习者数据对象没有对应文件或记录，是合法的空状态。
_Avoid_: 空、损坏

**Broken（损坏）**:
预期学习者数据对象存在，但无法解析或未通过数据契约；不能被读成空值或默认状态。
_Avoid_: 坏档、异常数据

**Partial（部分完成）**:
生成管线已产出部分有效产物，但未完成全部必需阶段的终态；不是 done。
_Avoid_: 成功、带错完成

**Generated（已生成）**:
课时存在至少一节 ready 正文，点开即有内容可读；数据源是节清单而非管线终态。
_Avoid_: 有内容、已完成、done

**Standard Layout（标准布局）**:
学习模块内容面的默认呈现模式：内容列居中限宽，课时正文行宽按连续阅读标准收窄。未设置宽度偏好时即为此模式。
_Avoid_: 默认布局、紧凑布局

**Wide Layout（满宽布局）**:
学习模块内容面的可选呈现模式：内容列不再居中限宽，课时阅读区由一整块带边框的面板占住整行；正文行宽随之放宽，但不放弃可读上限——余宽由结构与边框承担，而不是由更长的文字行承担。是用户可切换的偏好，不是每页写死的值。
_Avoid_: 全屏、铺满、宽敞（未指明是宽度还是密度）
