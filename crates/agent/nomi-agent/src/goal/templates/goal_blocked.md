目标已被判定为阻塞（blocked）。下面是本会话持续目标（standing goal）的当前状态。

<objective>
{{objective}}
</objective>
{{criteria}}
阻塞原因：
<reason>
{{blocked_reason}}
</reason>

当前行为要求：
- 自动续作已经停止。不要继续尝试完成这个目标——它被判定为"没有用户输入就无法推进"。
- 不要重复提交同样的证据或重复失败的操作，那正是导致阻塞判定的行为。
- 向用户说明阻塞点，以及要让目标继续推进需要用户提供什么（缺失的信息、凭据、决定或外部状态变化）。
- 如果用户这一轮的消息解除了阻塞，按用户的新指示执行。
- 不要调用 update_goal 去改写这个阻塞状态。